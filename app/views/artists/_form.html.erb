<%= form_for(@artist) do |f| %>
  <% if @artist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@artist.errors.count, "error") %> prohibited this artist from being saved:</h2>

      <ul>
      <% @artist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :date_start %><br>
    <%= f.datetime_select :date_start, start_year: 1900, :include_blank => true, :default => nil  %>
  </div>
  <div class="field">
    <%= f.label :date_end %><br>
    <%= f.datetime_select :date_end, start_year: 1900, :include_blank => true, :default => nil %>
  </div>
  <div class="field">
    <%= f.label :country_id %><br>
    <%= f.select(:country_id, options_for_select(@countries)) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

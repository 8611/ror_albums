<%= form_for(@artist) do |f| %>
  <% if @artist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@artist.errors.count, "error") %> prohibited this artist from being saved:</h2>

      <ul>
      <% @artist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
    <%= f.label :name %><br>
    <%= f.text_field :name, :class=>"form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :date_start %><br>
    <%= f.datetime_select :date_start, start_year: 1900, :include_blank => true, :default => nil, :class=>"form-control"  %>
  </div>
  <div class="form-group">
    <%= f.label :date_end %><br>
    <%= f.datetime_select :date_end, start_year: 1900, :include_blank => true, :default => nil, :class=>"form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :country_id %><br>
    <%= f.select(:country_id, options_for_select(@countries), {}, :class=>"form-control") %>
  </div>
  <div class="actions">
    <%= f.submit(:class=>"btn btn-primary") %>
  </div>
<% end %>
